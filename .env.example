# LocalStack設定
LOCALSTACK_SERVICES=dynamodb,iam,lambda,apigateway,logs
LOCALSTACK_PORT=4666
LOCALSTACK_DEBUG=0

# AWS CLI設定（LocalStack用）
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1

# Dynamodb-admin設定
DYNAMODB_ADMIN_PORT=8001
