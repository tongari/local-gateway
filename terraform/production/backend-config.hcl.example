# Terraform Backend設定（サンプル）
#
# 使用方法:
# 1. このファイルを backend-config.hcl にコピー
#    cp backend-config.hcl.example backend-config.hcl
#
# 2. <ACCOUNT_ID> を実際のAWSアカウントIDに置き換え
#
# 3. terraform init で初期化
#    terraform init -backend-config=backend-config.hcl

bucket         = "local-gateway-tfstate-<ACCOUNT_ID>"
key            = "production/terraform.tfstate"
region         = "ap-northeast-1"
dynamodb_table = "local-gateway-tfstate-lock"
encrypt        = true
